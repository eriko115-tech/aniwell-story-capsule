{
  "name": "aniwell-story-capsule",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler pages dev .",
    "deploy": "wrangler pages deploy .",
    "db:apply:local": "wrangler d1 execute aniwell-story-capsule-db --local --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.24.3"
  }
}
